# Rust build output
**/target/

# wasm-pack output (rebuilt by build-npm.sh, published to npm)
wasm/pkg/

# UniFFI generated bindings (regenerated by uniffi-bindgen; the published copies
# are vendored into the SDK packages under sdks/)
uniffi/generated/
